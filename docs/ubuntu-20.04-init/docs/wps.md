# uninstall LibreOffice and install WPS

[WPS](https://linux.wps.cn/#)。

```shell
# uninstall LibreOffice
sudo apt-get remove --purge libreoffice\* #only for zsh
sudo apt-get clean
sudo apt-get autoremove
```
